# i18n (ES/EN) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add cookie-based Spanish/English internationalization with auto-detect from `Accept-Language` and a navbar toggle, using zero new dependencies.

**Architecture:** The proxy forwards a resolved `x-locale` header on every request; Server Components call `getDictionary(await getLocale())` directly; Client Components read translations from a `LocaleProvider` context injected by the root layout.

**Tech Stack:** Next.js 16 App Router, React 19, Vitest + @testing-library/react, zero new npm packages.

---

## File Map

| Action | Path | Responsibility |
|--------|------|----------------|
| Create | `src/messages/es.json` | All Spanish strings |
| Create | `src/messages/en.json` | All English strings |
| Create | `src/lib/i18n.ts` | `getLocale`, `getDictionary`, `t()` |
| Create | `src/components/locale-provider.tsx` | React Context + `useDict`, `useLocale` |
| Create | `src/components/locale-switcher.tsx` | ES/EN toggle button in navbar |
| Create | `src/test-utils/dict-provider.tsx` | `MockDictProvider` for component tests |
| Modify | `src/proxy.ts` | Inject `x-locale` header |
| Modify | `src/app/layout.tsx` | Add `LocaleProvider`, dynamic `lang` attr |
| Modify | `src/app/actions.ts` | Add `setLocaleAction` |
| Modify | `src/components/navbar.tsx` | Add `LocaleSwitcher`, translate strings |
| Modify | `src/components/nav-links.tsx` | Translate nav labels via `useDict` |
| Modify | `src/components/bottom-nav.tsx` | Translate tab labels + menu via `useDict` |
| Modify | `src/components/match-card.tsx` | Translate status badges + prediction strings |
| Modify | `src/components/invite-copy-button.tsx` | Translate copy/copied via `useDict` |
| Modify | `src/components/prediction-input.tsx` | Translate status text via `useDict` |
| Modify | `src/components/torneo-filters.tsx` | Translate filter labels + badge aria-labels via `useDict` |
| Modify | `src/app/login/page.tsx` | Translate all strings |
| Modify | `src/app/(shell)/reglas/page.tsx` | Translate all strings |
| Modify | `src/app/(shell)/torneo/page.tsx` | Translate stage labels |
| Modify | `src/app/(protected)/grupos/page.tsx` | Translate all strings |
| Modify | `src/app/(protected)/grupos/[id]/page.tsx` | Translate all strings |
| Modify | `src/app/(protected)/perfil/page.tsx` | Translate all strings |
| Modify | `src/app/(shell)/invite/[code]/page.tsx` | Translate all strings |
| Modify | `src/__tests__/middleware.test.ts` | Add locale resolver tests |
| Modify | `src/__tests__/bottom-nav.test.tsx` | Wrap with MockDictProvider |
| Modify | `src/__tests__/invite-copy-button.test.tsx` | Wrap with MockDictProvider, update string keys |
| Modify | `src/__tests__/prediction-input.test.tsx` | Wrap with MockDictProvider, update string keys |

---

## Task 1: Create translation files

**Files:**
- Create: `src/messages/es.json`
- Create: `src/messages/en.json`

- [ ] **Step 1: Create `src/messages/es.json`**

```json
{
  "nav": {
    "torneo": "Torneo",
    "grupos": "Grupos",
    "reglas": "Reglas",
    "perfil": "Perfil",
    "more": "Más",
    "signOut": "Salir",
    "signIn": "Iniciar sesión"
  },
  "login": {
    "title": "Prode Mundial 2026",
    "subtitle": "Jugá con tus amigos",
    "continueWithGoogle": "Continuar con Google",
    "terms": "Al ingresar aceptás las reglas del torneo",
    "previewLogin": "Preview login"
  },
  "grupos": {
    "title": "Mis grupos",
    "welcome": "Bienvenido, {name}",
    "emptyTitle": "Todavía no pertenecés a ningún grupo",
    "emptySubtitle": "Creá uno o pedile a alguien el código de invitación",
    "createTitle": "Crear grupo",
    "createPlaceholder": "Nombre del grupo",
    "createButton": "Crear",
    "joinTitle": "Unirse con código",
    "joinPlaceholder": "Código de invitación",
    "joinButton": "Unirse",
    "memberSingular": "participante",
    "memberPlural": "participantes"
  },
  "grupoDetail": {
    "back": "← Mis grupos",
    "memberSingular": "participante",
    "memberPlural": "participantes",
    "standingsTitle": "Tabla de posiciones",
    "tableRank": "#",
    "tableParticipant": "Participante",
    "tablePoints": "Pts",
    "tableCorrect": "Aciertos",
    "inviteTitle": "Invitar amigos",
    "copyButton": "Copiar",
    "copied": "¡Copiado!"
  },
  "torneo": {
    "stageGroup": "Fase de Grupos",
    "stageRound32": "Ronda de 32",
    "stageRound16": "Octavos",
    "stageQuarter": "Cuartos",
    "stageSemi": "Semifinales",
    "stageThird": "Tercer Puesto",
    "stageFinal": "Final",
    "groupLabel": "Grupo {name}",
    "filterAll": "Todos",
    "filterGroupLabel": "Grupo:",
    "badgeComplete": "Grupo completo",
    "badgeActionRequired": "Pronósticos pendientes",
    "badgeMissed": "Pronósticos perdidos"
  },
  "match": {
    "statusScheduled": "Programado",
    "statusInProgress": "En juego",
    "statusFinished": "Finalizado",
    "statusPostponed": "Postergado",
    "statusCancelled": "Cancelado",
    "yourPrediction": "Tu pronóstico:",
    "noPrediction": "Sin pronóstico",
    "winner": "ganador: {name}",
    "loginToPredict": "Iniciá sesión para hacer tu pronóstico →"
  },
  "prediction": {
    "tapToStart": "Tocá ▲ para empezar",
    "completeOther": "Completá el otro score",
    "locked": "Este partido ya cerró",
    "knockoutWinner": "Ganador en penales..."
  },
  "perfil": {
    "title": "Perfil",
    "notificationsTitle": "Notificaciones",
    "notificationsLabel": "Recordatorios y resumen diario",
    "notificationsDescription": "Te avisamos 2 horas antes de cada partido si todavía no cargaste pronóstico, y te mandamos un resumen de puntos al final del día.",
    "saveButton": "Guardar"
  },
  "invite": {
    "invalidTitle": "Link inválido",
    "invalidDescription": "Este link de invitación no es válido o ya no está disponible.",
    "viewTournament": "Ver el torneo →",
    "memberSingular": "participante",
    "memberPlural": "participantes",
    "loggedInDescription": "Uniéndote a este grupo podrás competir con sus miembros.",
    "guestDescription": "Iniciá sesión para unirte a este grupo y competir.",
    "joinButton": "Unirse al grupo",
    "loginToJoin": "Iniciar sesión para unirte"
  },
  "reglas": {
    "title": "Reglas de puntuación",
    "subtitle": "Cómo se calculan los puntos por cada partido.",
    "sectionGroups": "Fase de grupos",
    "sectionKnockout": "Fases eliminatorias",
    "sectionKnockoutSubtitle": "Misma puntuación que en grupos para el marcador, más un bonus por el clasificado.",
    "sectionSummary": "Resumen rápido",
    "colCondition": "Condición",
    "colPts": "Pts",
    "exactResult": "Resultado exacto",
    "exactResultDesc": "Acertás los dos marcadores. Ej: pronosticás 2-1 y sale 2-1.",
    "correctPlusOne": "Resultado correcto + un marcador exacto",
    "correctPlusOneDesc": "Acertás quién gana (o que empata) y uno de los dos goles. Ej: pronosticás 2-1, sale 2-0.",
    "correctResult": "Resultado correcto",
    "correctResultDesc": "Acertás quién gana o que empata, pero ningún marcador. Ej: pronosticás 2-1, sale 3-0.",
    "wrongResult": "Resultado incorrecto",
    "wrongResultDesc": "El equipo que dijiste que ganaba perdió, o dijiste empate y hubo ganador.",
    "exactPlusWinner": "Resultado exacto + clasificado correcto",
    "exactPlusWinnerDesc": "Ambos marcadores exactos y acertás quién avanza. Máximo posible.",
    "exactNoWinner": "Resultado exacto sin clasificado correcto",
    "exactNoWinnerDesc": "Pronosticás empate con marcador exacto (ej: 1-1) pero te equivocás en quién clasifica.",
    "correctPlusWinner": "Resultado correcto + clasificado correcto",
    "correctPlusWinnerDesc": "Acertás el resultado (sin marcadores exactos) y quién avanza.",
    "onlyCorrect": "Solo resultado correcto",
    "onlyCorrectDesc": "Acertás el resultado pero te equivocaste en el clasificado.",
    "bonusNote": "El marcador se evalúa al final del tiempo reglamentario (90 min). El clasificado puede diferir si el partido se define en prórroga o penales.",
    "baseResult": "Resultado correcto (base)",
    "bonusesNote": "Los siguientes bonuses solo aplican si primero acertás el resultado",
    "homeGoalExact": "Gol del local exacto",
    "awayGoalExact": "Gol del visitante exacto",
    "bothGoalsBonus": "Ambos goles exactos (bonus)",
    "winnerBonus": "Clasificado correcto (eliminatorias)",
    "maxPoints": "Máximo grupos / eliminatorias"
  }
}
```

- [ ] **Step 2: Create `src/messages/en.json`**

```json
{
  "nav": {
    "torneo": "Tournament",
    "grupos": "Groups",
    "reglas": "Rules",
    "perfil": "Profile",
    "more": "More",
    "signOut": "Sign out",
    "signIn": "Sign in"
  },
  "login": {
    "title": "World Cup 2026 Predictions",
    "subtitle": "Play with your friends",
    "continueWithGoogle": "Continue with Google",
    "terms": "By signing in you accept the tournament rules",
    "previewLogin": "Preview login"
  },
  "grupos": {
    "title": "My groups",
    "welcome": "Welcome, {name}",
    "emptyTitle": "You don't belong to any group yet",
    "emptySubtitle": "Create one or ask someone for an invite code",
    "createTitle": "Create group",
    "createPlaceholder": "Group name",
    "createButton": "Create",
    "joinTitle": "Join with code",
    "joinPlaceholder": "Invite code",
    "joinButton": "Join",
    "memberSingular": "member",
    "memberPlural": "members"
  },
  "grupoDetail": {
    "back": "← My groups",
    "memberSingular": "member",
    "memberPlural": "members",
    "standingsTitle": "Standings",
    "tableRank": "#",
    "tableParticipant": "Participant",
    "tablePoints": "Pts",
    "tableCorrect": "Correct",
    "inviteTitle": "Invite friends",
    "copyButton": "Copy",
    "copied": "Copied!"
  },
  "torneo": {
    "stageGroup": "Group Stage",
    "stageRound32": "Round of 32",
    "stageRound16": "Round of 16",
    "stageQuarter": "Quarter-finals",
    "stageSemi": "Semi-finals",
    "stageThird": "Third place",
    "stageFinal": "Final",
    "groupLabel": "Group {name}",
    "filterAll": "All",
    "filterGroupLabel": "Group:",
    "badgeComplete": "Complete group",
    "badgeActionRequired": "Predictions pending",
    "badgeMissed": "Predictions missed"
  },
  "match": {
    "statusScheduled": "Scheduled",
    "statusInProgress": "Live",
    "statusFinished": "Finished",
    "statusPostponed": "Postponed",
    "statusCancelled": "Cancelled",
    "yourPrediction": "Your prediction:",
    "noPrediction": "No prediction",
    "winner": "winner: {name}",
    "loginToPredict": "Sign in to make your prediction →"
  },
  "prediction": {
    "tapToStart": "Tap ▲ to start",
    "completeOther": "Complete the other score",
    "locked": "This match is locked",
    "knockoutWinner": "Winner on penalties..."
  },
  "perfil": {
    "title": "Profile",
    "notificationsTitle": "Notifications",
    "notificationsLabel": "Reminders and daily summary",
    "notificationsDescription": "We notify you 2 hours before each match if you haven't submitted a prediction yet, and send a daily points summary.",
    "saveButton": "Save"
  },
  "invite": {
    "invalidTitle": "Invalid link",
    "invalidDescription": "This invite link is not valid or is no longer available.",
    "viewTournament": "View tournament →",
    "memberSingular": "member",
    "memberPlural": "members",
    "loggedInDescription": "Joining this group lets you compete with its members.",
    "guestDescription": "Sign in to join this group and compete.",
    "joinButton": "Join group",
    "loginToJoin": "Sign in to join"
  },
  "reglas": {
    "title": "Scoring rules",
    "subtitle": "How points are calculated for each match.",
    "sectionGroups": "Group stage",
    "sectionKnockout": "Knockout rounds",
    "sectionKnockoutSubtitle": "Same score as group stage for the result, plus a bonus for the qualifier.",
    "sectionSummary": "Quick summary",
    "colCondition": "Condition",
    "colPts": "Pts",
    "exactResult": "Exact result",
    "exactResultDesc": "You get both scores right. E.g.: you predict 2-1 and it ends 2-1.",
    "correctPlusOne": "Correct result + one exact score",
    "correctPlusOneDesc": "You get the winner (or draw) and one of the two goals. E.g.: predict 2-1, ends 2-0.",
    "correctResult": "Correct result",
    "correctResultDesc": "You get the winner or draw, but neither score. E.g.: predict 2-1, ends 3-0.",
    "wrongResult": "Wrong result",
    "wrongResultDesc": "The team you picked to win lost, or you predicted a draw and there was a winner.",
    "exactPlusWinner": "Exact result + correct qualifier",
    "exactPlusWinnerDesc": "Both scores exact and you get the qualifier right. Maximum possible.",
    "exactNoWinner": "Exact result, wrong qualifier",
    "exactNoWinnerDesc": "You predict a draw with exact score (e.g. 1-1) but get the qualifier wrong.",
    "correctPlusWinner": "Correct result + correct qualifier",
    "correctPlusWinnerDesc": "You get the result (without exact scores) and the qualifier right.",
    "onlyCorrect": "Result only",
    "onlyCorrectDesc": "You get the result right but got the qualifier wrong.",
    "bonusNote": "The score is evaluated at the end of regular time (90 min). The qualifier may differ if the match is decided in extra time or penalties.",
    "baseResult": "Correct result (base)",
    "bonusesNote": "The following bonuses only apply if you first get the result right",
    "homeGoalExact": "Exact home goals",
    "awayGoalExact": "Exact away goals",
    "bothGoalsBonus": "Both goals exact (bonus)",
    "winnerBonus": "Correct qualifier (knockouts)",
    "maxPoints": "Max groups / knockouts"
  }
}
```

- [ ] **Step 3: Commit**

```bash
git add src/messages/es.json src/messages/en.json
git commit -m "feat: add es/en translation dictionaries"
```

---

## Task 2: Create `src/lib/i18n.ts` with unit tests

**Files:**
- Create: `src/lib/i18n.ts`
- Create: `src/__tests__/i18n.test.ts`

- [ ] **Step 1: Write the failing tests**

Create `src/__tests__/i18n.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { t, getDictionary, LOCALES, DEFAULT_LOCALE } from '@/lib/i18n'

describe('t() interpolation', () => {
  it('returns string unchanged when no vars', () => {
    expect(t('Hello world')).toBe('Hello world')
  })

  it('interpolates a single variable', () => {
    expect(t('Bienvenido, {name}', { name: 'Ana' })).toBe('Bienvenido, Ana')
  })

  it('interpolates a numeric variable', () => {
    expect(t('{count} participantes', { count: 5 })).toBe('5 participantes')
  })

  it('interpolates multiple variables', () => {
    expect(t('{a} y {b}', { a: 'uno', b: 'dos' })).toBe('uno y dos')
  })

  it('leaves unknown keys in place', () => {
    expect(t('Hello, {name}', {})).toBe('Hello, {name}')
  })
})

describe('getDictionary()', () => {
  it('returns a dictionary for "es" with expected top-level keys', async () => {
    const dict = await getDictionary('es')
    expect(dict).toHaveProperty('nav')
    expect(dict).toHaveProperty('login')
    expect(dict).toHaveProperty('grupos')
    expect(dict.nav.torneo).toBe('Torneo')
  })

  it('returns a dictionary for "en" with expected top-level keys', async () => {
    const dict = await getDictionary('en')
    expect(dict).toHaveProperty('nav')
    expect(dict).toHaveProperty('login')
    expect(dict.nav.torneo).toBe('Tournament')
  })
})

describe('constants', () => {
  it('LOCALES contains es and en', () => {
    expect(LOCALES).toContain('es')
    expect(LOCALES).toContain('en')
  })

  it('DEFAULT_LOCALE is es', () => {
    expect(DEFAULT_LOCALE).toBe('es')
  })
})
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
cd /Users/mariano/Work/prode && npx vitest run src/__tests__/i18n.test.ts
```

Expected: FAIL — `Cannot find module '@/lib/i18n'`

- [ ] **Step 3: Create `src/lib/i18n.ts`**

```ts
import 'server-only'
import { headers } from 'next/headers'

export type Locale = 'es' | 'en'
export const LOCALES: Locale[] = ['es', 'en']
export const DEFAULT_LOCALE: Locale = 'es'

const dictionaries = {
  es: () => import('@/messages/es.json').then((m) => m.default),
  en: () => import('@/messages/en.json').then((m) => m.default),
} satisfies Record<Locale, () => Promise<unknown>>

export type Dictionary = Awaited<ReturnType<typeof dictionaries['es']>>

export async function getDictionary(locale: Locale): Promise<Dictionary> {
  return dictionaries[locale]() as Promise<Dictionary>
}

export async function getLocale(): Promise<Locale> {
  const headersList = await headers()
  const locale = headersList.get('x-locale')
  if (locale === 'es' || locale === 'en') return locale
  return DEFAULT_LOCALE
}

export function t(str: string, vars?: Record<string, string | number>): string {
  if (!vars) return str
  return str.replace(/\{(\w+)\}/g, (_, key) => {
    const val = vars[key]
    return val !== undefined ? String(val) : `{${key}}`
  })
}
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
cd /Users/mariano/Work/prode && npx vitest run src/__tests__/i18n.test.ts
```

Expected: all 8 tests PASS

Note: `server-only` causes an import error in Vitest (jsdom). If tests fail with `"server-only" cannot be imported`, add this mock to `vitest.config.ts` under `test.server.deps` OR add an alias. The simpler fix: add to `vitest.config.ts`:

```ts
// inside defineConfig({ ... })
resolve: {
  alias: {
    '@': path.resolve(__dirname, './src'),
    'next/server': path.resolve(__dirname, 'node_modules/next/server.js'),
    'server-only': path.resolve(__dirname, 'src/__mocks__/server-only.ts'),
  },
},
```

And create `src/__mocks__/server-only.ts`:
```ts
export {}
```

Re-run after this fix.

- [ ] **Step 5: Commit**

```bash
git add src/lib/i18n.ts src/__tests__/i18n.test.ts src/__mocks__/server-only.ts vitest.config.ts
git commit -m "feat: add i18n utilities (getLocale, getDictionary, t)"
```

---

## Task 3: Update `src/proxy.ts` — inject locale header

**Files:**
- Modify: `src/proxy.ts`
- Modify: `src/__tests__/middleware.test.ts`

- [ ] **Step 1: Write the failing tests**

Add to the bottom of `src/__tests__/middleware.test.ts`:

```ts
// ─── Locale resolver ──────────────────────────────────────────────────────────

function resolveLocale(cookieValue: string | undefined, acceptLanguage: string): string {
  if (cookieValue === 'es' || cookieValue === 'en') return cookieValue
  const firstTag = acceptLanguage.split(',')[0].split(';')[0].trim()
  const code = firstTag.split('-')[0].toLowerCase()
  if (code === 'es' || code === 'en') return code
  return 'es'
}

describe('resolveLocale()', () => {
  it('returns cookie value when valid', () => {
    expect(resolveLocale('en', '')).toBe('en')
    expect(resolveLocale('es', '')).toBe('es')
  })

  it('ignores invalid cookie value and falls back to Accept-Language', () => {
    expect(resolveLocale('fr', 'en-US,en;q=0.9')).toBe('en')
  })

  it('parses Accept-Language with quality values', () => {
    expect(resolveLocale(undefined, 'en-US,en;q=0.9,es;q=0.8')).toBe('en')
  })

  it('parses Accept-Language with region tag', () => {
    expect(resolveLocale(undefined, 'es-AR,es;q=0.9')).toBe('es')
  })

  it('defaults to es when no match', () => {
    expect(resolveLocale(undefined, 'fr-FR,fr;q=0.9')).toBe('es')
  })

  it('defaults to es when Accept-Language is empty', () => {
    expect(resolveLocale(undefined, '')).toBe('es')
  })
})
```

- [ ] **Step 2: Run tests to verify they pass (logic is already inline)**

```bash
cd /Users/mariano/Work/prode && npx vitest run src/__tests__/middleware.test.ts
```

Expected: all tests PASS (the function is defined inline in the test file)

- [ ] **Step 3: Update `src/proxy.ts`**

```ts
import { auth } from "@/auth";
import { NextResponse } from "next/server";
import type { NextRequest } from "next/server";

export default auth((req) => {
  const isLoggedIn = !!req.auth;
  const { pathname } = req.nextUrl;
  const isPublic =
    pathname === "/" ||
    pathname === "/login" ||
    pathname === "/reglas" ||
    pathname === "/torneo" ||
    pathname.startsWith("/api/auth") ||
    pathname.startsWith("/invite/");

  if (!isLoggedIn && !isPublic) {
    return NextResponse.redirect(new URL("/login", req.url));
  }

  if (isLoggedIn && pathname === "/login") {
    return NextResponse.redirect(new URL("/grupos", req.url));
  }

  // Pass-through: inject resolved locale as request header
  const locale = resolveLocale(
    req.cookies.get("prode_locale")?.value,
    req.headers.get("accept-language") ?? ""
  );
  const headers = new Headers(req.headers);
  headers.set("x-locale", locale);
  return NextResponse.next({ request: { headers } });
});

export const config = {
  matcher: [
    "/((?!_next/static|_next/image|favicon.ico|.*\\.(?:png|jpg|jpeg|svg|ico|webp|gif|css|js|woff2?|ttf|eot|otf|txt|xml|json)$).*)",
  ],
};

function resolveLocale(
  cookieValue: string | undefined,
  acceptLanguage: string
): string {
  if (cookieValue === "es" || cookieValue === "en") return cookieValue;
  const firstTag = acceptLanguage.split(",")[0].split(";")[0].trim();
  const code = firstTag.split("-")[0].toLowerCase();
  if (code === "es" || code === "en") return code;
  return "es";
}
```

- [ ] **Step 4: Verify all middleware tests still pass**

```bash
cd /Users/mariano/Work/prode && npx vitest run src/__tests__/middleware.test.ts
```

Expected: all tests PASS

- [ ] **Step 5: Commit**

```bash
git add src/proxy.ts src/__tests__/middleware.test.ts
git commit -m "feat: inject x-locale header in proxy for i18n"
```

---

## Task 4: Create `LocaleProvider` context

**Files:**
- Create: `src/components/locale-provider.tsx`

No unit test for a thin context wrapper — it will be exercised by the component tests in later tasks.

- [ ] **Step 1: Create `src/components/locale-provider.tsx`**

```tsx
'use client'

import { createContext, useContext } from 'react'
import type { Dictionary } from '@/lib/i18n'

type LocaleContextValue = {
  locale: string
  dict: Dictionary
}

const LocaleContext = createContext<LocaleContextValue | null>(null)

export function LocaleProvider({
  locale,
  dict,
  children,
}: {
  locale: string
  dict: Dictionary
  children: React.ReactNode
}) {
  return (
    <LocaleContext.Provider value={{ locale, dict }}>
      {children}
    </LocaleContext.Provider>
  )
}

export function useDict(): Dictionary {
  const ctx = useContext(LocaleContext)
  if (!ctx) throw new Error('useDict must be used within LocaleProvider')
  return ctx.dict
}

export function useLocale(): string {
  const ctx = useContext(LocaleContext)
  if (!ctx) throw new Error('useLocale must be used within LocaleProvider')
  return ctx.locale
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/locale-provider.tsx
git commit -m "feat: add LocaleProvider context for client-side i18n"
```

---

## Task 5: Wire root layout + setLocaleAction

**Files:**
- Modify: `src/app/layout.tsx`
- Modify: `src/app/actions.ts`

- [ ] **Step 1: Add `setLocaleAction` to `src/app/actions.ts`**

```ts
'use server'

import { signOut } from '@/auth'
import { cookies } from 'next/headers'
import { revalidatePath } from 'next/cache'

export async function signOutAction() {
  await signOut({ redirectTo: '/login' })
}

export async function setLocaleAction(locale: string) {
  const cookieStore = await cookies()
  cookieStore.set('prode_locale', locale, {
    maxAge: 60 * 60 * 24 * 365,
    path: '/',
    sameSite: 'lax',
  })
  revalidatePath('/', 'layout')
}
```

- [ ] **Step 2: Update `src/app/layout.tsx`**

```tsx
import type { Metadata, Viewport } from 'next'
import { Inter } from 'next/font/google'
import { SpeedInsights } from '@vercel/speed-insights/next'
import { LocaleProvider } from '@/components/locale-provider'
import { getLocale, getDictionary } from '@/lib/i18n'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Prode Mundial 2026',
  description: 'Jugá al prode del Mundial FIFA 2026 con tus amigos',
  manifest: '/manifest.json',
  appleWebApp: {
    capable: true,
    statusBarStyle: 'default',
    title: 'Prode 2026',
  },
}

export const viewport: Viewport = {
  themeColor: '#2563eb',
}

export default async function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  const locale = await getLocale()
  const dict = await getDictionary(locale)

  return (
    <html lang={locale}>
      <body className={inter.className}>
        <LocaleProvider locale={locale} dict={dict}>
          {children}
        </LocaleProvider>
        <SpeedInsights />
      </body>
    </html>
  )
}
```

- [ ] **Step 3: Commit**

```bash
git add src/app/layout.tsx src/app/actions.ts
git commit -m "feat: wire LocaleProvider in root layout and add setLocaleAction"
```

---

## Task 6: Create `LocaleSwitcher` and update `Navbar`

**Files:**
- Create: `src/components/locale-switcher.tsx`
- Modify: `src/components/navbar.tsx`

- [ ] **Step 1: Create `src/components/locale-switcher.tsx`**

```tsx
'use client'

import { useLocale } from '@/components/locale-provider'
import { setLocaleAction } from '@/app/actions'

export function LocaleSwitcher() {
  const locale = useLocale()

  return (
    <div className="flex items-center gap-0.5">
      {(['es', 'en'] as const).map((l) => (
        <form key={l} action={setLocaleAction.bind(null, l)}>
          <button
            type="submit"
            className="text-xs font-semibold px-2 py-1 rounded transition-colors uppercase"
            style={{
              color: locale === l ? 'var(--text-primary)' : 'var(--text-muted)',
              background: locale === l ? 'var(--surface-raised)' : 'transparent',
              border: locale === l ? '1px solid var(--border)' : '1px solid transparent',
            }}
          >
            {l}
          </button>
        </form>
      ))}
    </div>
  )
}
```

- [ ] **Step 2: Update `src/components/navbar.tsx`**

Replace the entire file:

```tsx
// src/components/navbar.tsx
import Link from 'next/link'
import Image from 'next/image'
import { auth, signOut } from '@/auth'
import { SubmitButton } from '@/components/submit-button'
import { NavLinks } from '@/components/nav-links'
import { LocaleSwitcher } from '@/components/locale-switcher'
import { getLocale, getDictionary } from '@/lib/i18n'

export default async function Navbar() {
  const session = await auth()
  const locale = await getLocale()
  const dict = await getDictionary(locale)

  return (
    <nav
      className="px-4 py-3 flex items-center justify-between border-b"
      style={{ background: 'var(--surface)', borderColor: 'var(--border)' }}
    >
      <Link
        href={session ? '/torneo' : '/'}
        className="font-extrabold text-base flex items-center gap-2 tracking-tight"
        style={{ color: 'var(--text-primary)' }}
      >
        <span>⚽</span>
        <span>Prode 2026</span>
        <span
          className="text-[9px] font-black px-1.5 py-0.5 rounded-full tracking-wide"
          style={{ background: 'var(--accent)', color: '#000' }}
        >
          FIFA
        </span>
      </Link>

      {session?.user ? (
        <div className="flex items-center gap-2 md:gap-4">
          <LocaleSwitcher />
          <div className="hidden md:flex items-center gap-4">
            <NavLinks isSuperAdmin={session.user.isSuperAdmin ?? false} />
          </div>
          {session.user.image && (
            <Image
              src={session.user.image}
              alt={session.user.name ?? ''}
              width={30}
              height={30}
              className="rounded-full border"
              style={{ borderColor: 'var(--border)' }}
            />
          )}
          <Link
            href="/perfil"
            className="hidden md:block text-sm"
            style={{ color: 'var(--text-muted)' }}
          >
            {session.user.name}
          </Link>
          <form
            action={async () => {
              'use server'
              await signOut({ redirectTo: '/login' })
            }}
          >
            <SubmitButton
              className="hidden md:block text-sm transition-colors"
              style={{ color: 'var(--text-muted)' } as React.CSSProperties}
            >
              {dict.nav.signOut}
            </SubmitButton>
          </form>
        </div>
      ) : (
        <div className="flex items-center gap-4">
          <LocaleSwitcher />
          {(['torneo', 'reglas'] as const).map((key) => (
            <Link
              key={key}
              href={`/${key}`}
              className="text-sm"
              style={{ color: 'var(--text-muted)' }}
            >
              {dict.nav[key]}
            </Link>
          ))}
          <Link
            href="/login"
            className="text-sm"
            style={{ color: 'var(--text-muted)' }}
          >
            {dict.nav.signIn}
          </Link>
        </div>
      )}
    </nav>
  )
}
```

- [ ] **Step 3: Commit**

```bash
git add src/components/locale-switcher.tsx src/components/navbar.tsx
git commit -m "feat: add LocaleSwitcher to navbar"
```

---

## Task 7: `MockDictProvider` + translate `BottomNav` and `NavLinks`

**Files:**
- Create: `src/test-utils/dict-provider.tsx`
- Modify: `src/components/bottom-nav.tsx`
- Modify: `src/components/nav-links.tsx`
- Modify: `src/__tests__/bottom-nav.test.tsx`

- [ ] **Step 1: Create `src/test-utils/dict-provider.tsx`**

```tsx
import { LocaleProvider } from '@/components/locale-provider'
import esDict from '@/messages/es.json'
import type { Dictionary } from '@/lib/i18n'

export function MockDictProvider({ children }: { children: React.ReactNode }) {
  return (
    <LocaleProvider locale="es" dict={esDict as unknown as Dictionary}>
      {children}
    </LocaleProvider>
  )
}
```

- [ ] **Step 2: Update `src/__tests__/bottom-nav.test.tsx`** to wrap with `MockDictProvider`

Replace the full file:

```tsx
import { describe, it, expect, vi } from 'vitest'
import { render, screen, fireEvent } from '@testing-library/react'
import BottomNav from '@/components/bottom-nav'
import { MockDictProvider } from '@/test-utils/dict-provider'

vi.mock('next/navigation', () => ({
  usePathname: vi.fn(() => '/torneo'),
}))

vi.mock('@/app/actions', () => ({
  signOutAction: vi.fn(),
}))

function renderBottomNav(isSuperAdmin = false) {
  return render(
    <MockDictProvider>
      <BottomNav isSuperAdmin={isSuperAdmin} />
    </MockDictProvider>
  )
}

describe('BottomNav', () => {
  it('renderiza los 4 tabs principales', () => {
    renderBottomNav()
    expect(screen.getByText('Torneo')).toBeDefined()
    expect(screen.getByText('Grupos')).toBeDefined()
    expect(screen.getByText('Reglas')).toBeDefined()
    expect(screen.getByText('Perfil')).toBeDefined()
  })

  it('renderiza el botón Más', () => {
    renderBottomNav()
    expect(screen.getByText('Más')).toBeDefined()
  })

  it('al hacer click en Más, muestra el sheet con Salir', () => {
    renderBottomNav()
    fireEvent.click(screen.getByText('Más'))
    expect(screen.getByText('Salir')).toBeDefined()
  })

  it('no muestra Admin si isSuperAdmin es false', () => {
    renderBottomNav()
    fireEvent.click(screen.getByText('Más'))
    expect(screen.queryByText('Admin')).toBeNull()
  })

  it('muestra Admin si isSuperAdmin es true', () => {
    renderBottomNav(true)
    fireEvent.click(screen.getByText('Más'))
    expect(screen.getByText('Admin')).toBeDefined()
  })

  it('cierra el sheet al hacer click en el overlay', () => {
    renderBottomNav()
    fireEvent.click(screen.getByText('Más'))
    expect(screen.getByText('Salir')).toBeDefined()
    const overlay = document.querySelector('[data-testid="more-overlay"]')!
    fireEvent.click(overlay)
    expect(screen.queryByText('Salir')).toBeNull()
  })
})
```

- [ ] **Step 3: Run tests — they will fail because BottomNav still has hardcoded strings outside context**

```bash
cd /Users/mariano/Work/prode && npx vitest run src/__tests__/bottom-nav.test.tsx
```

Expected: FAIL — `useDict must be used within LocaleProvider` (or similar) if BottomNav already uses the hook, otherwise PASS since strings are still hardcoded. Either way, proceed to Step 4.

- [ ] **Step 4: Update `src/components/bottom-nav.tsx`**

```tsx
// src/components/bottom-nav.tsx
'use client'

import Link from 'next/link'
import { usePathname } from 'next/navigation'
import { useState } from 'react'
import { signOutAction } from '@/app/actions'
import { SubmitButton } from '@/components/submit-button'
import { useDict } from '@/components/locale-provider'

export default function BottomNav({ isSuperAdmin }: { isSuperAdmin: boolean }) {
  const pathname = usePathname()
  const [moreOpen, setMoreOpen] = useState(false)
  const dict = useDict()

  const TABS = [
    { href: '/torneo', label: dict.nav.torneo, icon: '🏟️' },
    { href: '/grupos', label: dict.nav.grupos, icon: '👥' },
    { href: '/reglas', label: dict.nav.reglas, icon: '📋' },
    { href: '/perfil', label: dict.nav.perfil, icon: '👤' },
  ]

  return (
    <>
      {moreOpen && (
        <div
          data-testid="more-overlay"
          className="fixed inset-0 z-40 md:hidden"
          onClick={() => setMoreOpen(false)}
        />
      )}

      {moreOpen && (
        <div
          className="fixed bottom-16 right-0 z-50 border rounded-tl-xl shadow-lg min-w-40 md:hidden"
          style={{ background: 'var(--surface)', borderColor: 'var(--border)' }}
        >
          {isSuperAdmin && (
            <Link
              href="/admin/partidos"
              className="flex items-center gap-2 px-4 py-3 text-sm border-b"
              style={{ color: '#ef4444', borderColor: 'var(--border)' }}
              onClick={() => setMoreOpen(false)}
            >
              Admin
            </Link>
          )}
          <form action={signOutAction}>
            <SubmitButton
              className="flex items-center gap-2 px-4 py-3 text-sm w-full text-left"
              style={{ color: 'var(--text-muted)' } as React.CSSProperties}
            >
              {dict.nav.signOut}
            </SubmitButton>
          </form>
        </div>
      )}

      <nav
        className="fixed bottom-0 left-0 right-0 z-50 border-t md:hidden"
        style={{ background: 'var(--surface)', borderColor: 'var(--border)' }}
      >
        <div className="flex justify-around items-center py-1 pb-2">
          {TABS.map((tab) => {
            const isActive = pathname.startsWith(tab.href)
            return (
              <Link
                key={tab.href}
                href={tab.href}
                className="flex flex-col items-center gap-0.5 px-3 py-2 min-w-14 text-xs"
                style={{ color: isActive ? 'var(--accent)' : 'var(--text-muted)' }}
              >
                <span className="text-xl leading-none">{tab.icon}</span>
                <span className={isActive ? 'font-semibold' : ''}>{tab.label}</span>
                {isActive && (
                  <span
                    className="w-1 h-1 rounded-full mt-0.5"
                    style={{ background: 'var(--accent)' }}
                  />
                )}
              </Link>
            )
          })}

          <button
            onClick={() => setMoreOpen(!moreOpen)}
            className="flex flex-col items-center gap-0.5 px-3 py-2 min-w-14 text-xs"
            style={{ color: moreOpen ? 'var(--accent)' : 'var(--text-muted)' }}
          >
            <span className="text-xl leading-none">···</span>
            <span>{dict.nav.more}</span>
          </button>
        </div>
      </nav>
    </>
  )
}
```

- [ ] **Step 5: Update `src/components/nav-links.tsx`**

```tsx
'use client'

import Link from 'next/link'
import { usePathname } from 'next/navigation'
import { useDict } from '@/components/locale-provider'

export function NavLinks({ isSuperAdmin }: { isSuperAdmin: boolean }) {
  const pathname = usePathname()
  const dict = useDict()

  const LINKS = [
    { href: '/torneo', label: dict.nav.torneo },
    { href: '/grupos', label: dict.nav.grupos },
    { href: '/reglas', label: dict.nav.reglas },
  ]

  return (
    <div className="flex items-center gap-4">
      {LINKS.map(({ href, label }) => {
        const isActive = pathname.startsWith(href)
        return (
          <Link
            key={href}
            href={href}
            className="text-sm font-medium transition-colors"
            style={{ color: isActive ? 'var(--text-primary)' : 'var(--text-muted)' }}
          >
            {label}
          </Link>
        )
      })}
      {isSuperAdmin && (
        <Link
          href="/admin/partidos"
          className="text-sm font-medium transition-colors"
          style={{ color: pathname.startsWith('/admin') ? '#ef4444' : '#f87171' }}
        >
          Admin
        </Link>
      )}
    </div>
  )
}
```

- [ ] **Step 6: Run bottom-nav tests**

```bash
cd /Users/mariano/Work/prode && npx vitest run src/__tests__/bottom-nav.test.tsx
```

Expected: all 6 tests PASS

- [ ] **Step 7: Commit**

```bash
git add src/test-utils/dict-provider.tsx src/components/bottom-nav.tsx src/components/nav-links.tsx src/__tests__/bottom-nav.test.tsx
git commit -m "feat: translate BottomNav and NavLinks, add MockDictProvider"
```

---

## Task 8: Translate `InviteCopyButton`

**Files:**
- Modify: `src/components/invite-copy-button.tsx`
- Modify: `src/__tests__/invite-copy-button.test.tsx`

- [ ] **Step 1: Update `src/__tests__/invite-copy-button.test.tsx`**

Replace the full file:

```tsx
import { render, screen, fireEvent, act } from '@testing-library/react'
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { InviteCopyButton } from '@/components/invite-copy-button'
import { MockDictProvider } from '@/test-utils/dict-provider'

const mockWriteText = vi.fn().mockResolvedValue(undefined)

beforeEach(() => {
  vi.stubGlobal('navigator', { clipboard: { writeText: mockWriteText } })
  vi.stubGlobal('window', { location: { origin: 'https://prode.app' } })
  mockWriteText.mockClear()
})

function renderButton() {
  return render(
    <MockDictProvider>
      <InviteCopyButton inviteCode="abc123" />
    </MockDictProvider>
  )
}

describe('InviteCopyButton', () => {
  it('renders copy button', () => {
    renderButton()
    expect(screen.getByRole('button', { name: 'Copiar' })).toBeInTheDocument()
  })

  it('calls clipboard with full URL on click', async () => {
    renderButton()
    await act(async () => {
      fireEvent.click(screen.getByRole('button', { name: 'Copiar' }))
    })
    expect(mockWriteText).toHaveBeenCalledWith('https://prode.app/invite/abc123')
  })

  it('shows ¡Copiado! after copy', async () => {
    vi.useFakeTimers()
    renderButton()
    await act(async () => {
      fireEvent.click(screen.getByRole('button', { name: 'Copiar' }))
    })
    expect(screen.getByRole('button', { name: '¡Copiado!' })).toBeInTheDocument()
    vi.useRealTimers()
  })

  it('resets button to Copiar after 2 seconds', async () => {
    vi.useFakeTimers()
    renderButton()
    await act(async () => {
      fireEvent.click(screen.getByRole('button', { name: 'Copiar' }))
    })
    expect(screen.getByRole('button', { name: '¡Copiado!' })).toBeInTheDocument()
    await act(async () => {
      vi.advanceTimersByTime(2001)
    })
    expect(screen.getByRole('button', { name: 'Copiar' })).toBeInTheDocument()
    vi.useRealTimers()
  })
})
```

- [ ] **Step 2: Run test — should still pass (strings unchanged yet)**

```bash
cd /Users/mariano/Work/prode && npx vitest run src/__tests__/invite-copy-button.test.tsx
```

- [ ] **Step 3: Update `src/components/invite-copy-button.tsx`**

```tsx
'use client'

import { useState, useEffect, useRef } from 'react'
import { useDict } from '@/components/locale-provider'

export function InviteCopyButton({ inviteCode }: { inviteCode: string }) {
  const [copied, setCopied] = useState(false)
  const [url, setUrl] = useState('')
  const timeoutRef = useRef<ReturnType<typeof setTimeout> | null>(null)
  const dict = useDict()

  useEffect(() => {
    setUrl(`${window.location.origin}/invite/${inviteCode}`)
  }, [inviteCode])

  useEffect(() => {
    return () => {
      if (timeoutRef.current) clearTimeout(timeoutRef.current)
    }
  }, [])

  async function handleCopy() {
    if (!url) return
    try {
      await navigator.clipboard.writeText(url)
      setCopied(true)
      timeoutRef.current = setTimeout(() => setCopied(false), 2000)
    } catch (err) {
      console.error('Failed to copy invite URL:', err)
    }
  }

  return (
    <div
      className="rounded-xl px-4 py-3"
      style={{ background: 'var(--surface)', border: '1px solid var(--border)' }}
    >
      <p className="text-xs mb-2" style={{ color: 'var(--text-muted)' }}>
        {dict.grupoDetail.inviteTitle}
      </p>
      <div className="flex items-center gap-2">
        <span
          className="text-sm font-mono truncate flex-1 min-w-0"
          style={{ color: 'var(--text-primary)' }}
        >
          {url || `…/invite/${inviteCode}`}
        </span>
        <button
          onClick={handleCopy}
          disabled={copied}
          className="text-xs px-3 py-1.5 rounded-lg shrink-0 font-medium cursor-pointer transition-colors"
          style={{ background: 'var(--accent)', color: '#000' }}
        >
          {copied ? dict.grupoDetail.copied : dict.grupoDetail.copyButton}
        </button>
      </div>
    </div>
  )
}
```

- [ ] **Step 4: Run tests**

```bash
cd /Users/mariano/Work/prode && npx vitest run src/__tests__/invite-copy-button.test.tsx
```

Expected: all 4 tests PASS

- [ ] **Step 5: Commit**

```bash
git add src/components/invite-copy-button.tsx src/__tests__/invite-copy-button.test.tsx
git commit -m "feat: translate InviteCopyButton"
```

---

## Task 9: Translate `PredictionInput`

**Files:**
- Modify: `src/components/prediction-input.tsx`
- Modify: `src/__tests__/prediction-input.test.tsx`

- [ ] **Step 1: Update `src/__tests__/prediction-input.test.tsx`** — add MockDictProvider to all renders

Replace every `render(<PredictionInput {...} />)` with:

```tsx
render(
  <MockDictProvider>
    <PredictionInput {...} />
  </MockDictProvider>
)
```

At the top of the file, add:
```tsx
import { MockDictProvider } from '@/test-utils/dict-provider'
```

The full updated file:

```tsx
// src/__tests__/prediction-input.test.tsx
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen, fireEvent } from '@testing-library/react'
import { PredictionInput } from '@/components/prediction-input'
import { MockDictProvider } from '@/test-utils/dict-provider'

vi.mock('@/app/(protected)/torneo/actions', () => ({
  savePrediction: vi.fn().mockResolvedValue(undefined),
  deletePrediction: vi.fn().mockResolvedValue(undefined),
}))

const homeTeam = { flag: '🇦🇷', name: 'Argentina' }
const awayTeam = { flag: '🇧🇷', name: 'Brasil' }

const baseProps = {
  matchId: 'match-1',
  prediction: null,
  homeTeam,
  awayTeam,
  homeTeamId: 'home-1',
  awayTeamId: 'away-1',
  isKnockout: false,
  scheduledAt: new Date(Date.now() + 3600 * 1000).toISOString(),
}

function renderInput(props = baseProps) {
  return render(
    <MockDictProvider>
      <PredictionInput {...props} />
    </MockDictProvider>
  )
}

describe('PredictionInput', () => {
  beforeEach(() => { vi.clearAllMocks() })

  it('muestra — cuando no hay pronóstico', () => {
    renderInput()
    const dashes = screen.getAllByText('—')
    expect(dashes.length).toBeGreaterThanOrEqual(2)
  })

  it('muestra hint cuando no hay pronóstico ni scores', () => {
    renderInput()
    expect(screen.getByText('Tocá ▲ para empezar')).toBeDefined()
  })

  it('incrementa el score local al hacer click en ▲ del equipo local', () => {
    renderInput()
    const upButtons = screen.getAllByLabelText('Aumentar')
    fireEvent.click(upButtons[0])
    expect(screen.getByText('0')).toBeDefined()
  })

  it('segundo click en ▲ va a 1', () => {
    renderInput()
    const upButtons = screen.getAllByLabelText('Aumentar')
    fireEvent.click(upButtons[0])
    fireEvent.click(upButtons[0])
    expect(screen.getByText('1')).toBeDefined()
  })

  it('▼ no hace nada cuando el score es null', () => {
    renderInput()
    const downButtons = screen.getAllByLabelText('Disminuir')
    fireEvent.click(downButtons[0])
    const dashes = screen.getAllByText('—')
    expect(dashes.length).toBeGreaterThanOrEqual(2)
  })
})
```

Note: read the full existing test file first and carry over all remaining test cases beyond the 5 shown above, wrapping each render with `MockDictProvider`.

- [ ] **Step 2: Run tests — should still pass**

```bash
cd /Users/mariano/Work/prode && npx vitest run src/__tests__/prediction-input.test.tsx
```

- [ ] **Step 3: Update `src/components/prediction-input.tsx`**

Add `useDict` import and replace hardcoded status strings. Only the `statusText` object and the knockout `<select>` placeholder need changes. Replace these two sections in the file:

**Replace the `statusText` object** (around line 123–129):

```tsx
// Before:
const statusText: string | null = {
  idle: hasPrediction ? null : 'Tocá ▲ para empezar',
  partial: 'Completá el otro score',
  saving: null,
  saved: null,
  locked: 'Este partido ya cerró',
}[status]

// After:
const dict = useDict()
const statusText: string | null = {
  idle: hasPrediction ? null : dict.prediction.tapToStart,
  partial: dict.prediction.completeOther,
  saving: null,
  saved: null,
  locked: dict.prediction.locked,
}[status]
```

**Replace the knockout `<select>` placeholder** (around line 216):

```tsx
// Before:
<option value="">Ganador en penales...</option>

// After:
<option value="">{dict.prediction.knockoutWinner}</option>
```

Add the import at the top of the file:

```tsx
import { useDict } from '@/components/locale-provider'
```

- [ ] **Step 4: Run tests**

```bash
cd /Users/mariano/Work/prode && npx vitest run src/__tests__/prediction-input.test.tsx
```

Expected: all tests PASS

- [ ] **Step 5: Commit**

```bash
git add src/components/prediction-input.tsx src/__tests__/prediction-input.test.tsx
git commit -m "feat: translate PredictionInput status strings"
```

---

## Task 10: Translate `MatchCard` (Server Component)

**Files:**
- Modify: `src/components/match-card.tsx`

`MatchCard` is a Server Component that receives its data as props. The simplest approach: add a `dict` prop so the calling page passes its already-loaded dictionary.

- [ ] **Step 1: Update `src/components/match-card.tsx`**

Add `dict` to the `Props` type and replace hardcoded strings:

```tsx
// src/components/match-card.tsx
import { MatchStage, MatchStatus } from '@prisma/client'
import { ClientDate } from '@/components/client-date'
import { PredictionInput } from '@/components/prediction-input'
import Link from 'next/link'
import type { Dictionary } from '@/lib/i18n'

type Team = { flag: string; name: string } | null

type Match = {
  id: string
  stage: MatchStage
  status: MatchStatus
  scheduledAt: Date
  homeScore: number | null
  awayScore: number | null
  homeTeamId: string | null
  awayTeamId: string | null
  groupName: string | null
  homeTeam: Team
  awayTeam: Team
}

type Prediction = {
  homeScore: number
  awayScore: number
  predictedWinnerId: string | null
  points: number | null
} | null

type Props = {
  match: Match
  prediction: Prediction
  hasSession: boolean
  showGroupLabel: boolean
  locked: boolean
  isKnockout: boolean
  dict: Dictionary
}

export function MatchCard({ match, prediction, hasSession, showGroupLabel, locked, isKnockout, dict }: Props) {
  const badge = {
    SCHEDULED:   { label: dict.match.statusScheduled, bg: 'var(--surface-raised)', color: 'var(--text-muted)' },
    IN_PROGRESS: { label: dict.match.statusInProgress, bg: '#fbbf2422',            color: '#fbbf24' },
    FINISHED:    { label: dict.match.statusFinished,   bg: '#22c55e1a',             color: 'var(--accent)' },
    POSTPONED:   { label: dict.match.statusPostponed,  bg: '#ef44441a',             color: '#ef4444' },
    CANCELLED:   { label: dict.match.statusCancelled,  bg: '#ef44441a',             color: '#ef4444' },
  }[match.status]

  const hasPrediction = prediction !== null

  return (
    <div
      className="rounded-xl px-4 py-3 space-y-3"
      style={{
        background: 'var(--surface)',
        border: `1px solid ${hasPrediction && !locked ? '#22c55e40' : 'var(--border)'}`,
      }}
    >
      {/* Header row */}
      <div className="flex items-center justify-between gap-4">
        <div className="flex flex-col min-w-0">
          {showGroupLabel && match.groupName && (
            <span className="text-[10px] mb-0.5 uppercase tracking-wide" style={{ color: 'var(--text-muted)' }}>
              {dict.torneo.groupLabel.replace('{name}', match.groupName)}
            </span>
          )}
          <div className="flex items-center gap-3">
            <span className="text-sm font-medium truncate" style={{ color: 'var(--text-primary)' }}>
              {match.homeTeam ? `${match.homeTeam.flag} ${match.homeTeam.name}` : 'TBD'}
            </span>
            <span className="text-sm font-bold tabular-nums shrink-0" style={{ color: 'var(--text-muted)' }}>
              {match.status === 'FINISHED'
                ? `${match.homeScore} - ${match.awayScore}`
                : 'vs'}
            </span>
            <span className="text-sm font-medium truncate" style={{ color: 'var(--text-primary)' }}>
              {match.awayTeam ? `${match.awayTeam.flag} ${match.awayTeam.name}` : 'TBD'}
            </span>
          </div>
        </div>
        <div className="flex flex-col items-end shrink-0 gap-1">
          <span
            className="text-[10px] px-2 py-0.5 rounded-full font-semibold"
            style={{ background: badge.bg, color: badge.color }}
          >
            {badge.label}
          </span>
          <span className="text-[11px]" style={{ color: 'var(--text-muted)' }}>
            <ClientDate iso={match.scheduledAt.toISOString()} />
          </span>
        </div>
      </div>

      {/* Bottom row: prediction area */}
      {match.status !== 'CANCELLED' && (
        <div className="border-t pt-3" style={{ borderColor: 'var(--border)' }}>
          {locked ? (
            prediction ? (
              <div className="flex items-center gap-2 text-sm flex-wrap">
                <span className="text-xs" style={{ color: 'var(--text-muted)' }}>{dict.match.yourPrediction}</span>
                <span className="font-mono font-semibold" style={{ color: 'var(--text-primary)' }}>
                  {prediction.homeScore} - {prediction.awayScore}
                </span>
                {isKnockout && prediction.predictedWinnerId && (
                  <span className="text-xs" style={{ color: 'var(--text-muted)' }}>
                    {'('}
                    {dict.match.winner.replace('{name}',
                      prediction.predictedWinnerId === match.homeTeamId
                        ? (match.homeTeam?.name ?? '')
                        : (match.awayTeam?.name ?? '')
                    )}
                    {')'}
                  </span>
                )}
                {prediction.points !== null && (
                  <span
                    className="ml-auto text-xs font-bold px-2 py-0.5 rounded-full"
                    style={{
                      background: prediction.points > 0 ? '#22c55e1a' : 'var(--surface-raised)',
                      color: prediction.points > 0 ? 'var(--accent)' : 'var(--text-muted)',
                    }}
                  >
                    {prediction.points > 0 ? '+' : ''}{prediction.points} pts
                  </span>
                )}
              </div>
            ) : (
              <span className="text-xs" style={{ color: 'var(--text-muted)' }}>{dict.match.noPrediction}</span>
            )
          ) : hasSession ? (
            <PredictionInput
              key={prediction ? 'has-pred' : `new-${match.id}`}
              matchId={match.id}
              prediction={prediction}
              homeTeam={match.homeTeam}
              awayTeam={match.awayTeam}
              homeTeamId={match.homeTeamId}
              awayTeamId={match.awayTeamId}
              isKnockout={isKnockout}
              scheduledAt={match.scheduledAt.toISOString()}
            />
          ) : (
            <Link href="/login" className="text-xs" style={{ color: '#3b82f6' }}>
              {dict.match.loginToPredict}
            </Link>
          )}
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 2: Find all callers of `MatchCard` and add the `dict` prop**

The `MatchCard` component is used in `src/app/(shell)/torneo/page.tsx` and `src/app/(protected)/grupos/[id]/page.tsx`. Both are addressed in Task 11. For now, TypeScript will error on those files until Task 11 is done — that is expected.

- [ ] **Step 3: Commit**

```bash
git add src/components/match-card.tsx
git commit -m "feat: translate MatchCard, accept dict prop"
```

---

## Task 11: Translate server pages

**Files:**
- Modify: `src/app/login/page.tsx`
- Modify: `src/app/(shell)/reglas/page.tsx`
- Modify: `src/app/(shell)/torneo/page.tsx`
- Modify: `src/app/(protected)/grupos/page.tsx`
- Modify: `src/app/(protected)/grupos/[id]/page.tsx`
- Modify: `src/app/(protected)/perfil/page.tsx`
- Modify: `src/app/(shell)/invite/[code]/page.tsx`

Each page follows the same pattern: import `getLocale` + `getDictionary` from `@/lib/i18n`, call them at the top of the component, then replace hardcoded strings with `dict.*` references.

- [ ] **Step 1: Update `src/app/login/page.tsx`**

```tsx
// src/app/login/page.tsx
import { auth, signIn } from '@/auth'
import { redirect } from 'next/navigation'
import { getLocale, getDictionary } from '@/lib/i18n'

export default async function LoginPage({
  searchParams,
}: {
  searchParams: Promise<{ callbackUrl?: string }>
}) {
  const session = await auth()
  if (session) redirect('/grupos')

  const { callbackUrl } = await searchParams
  const isValid = callbackUrl?.startsWith('/') && !callbackUrl.startsWith('//')
  const redirectTo = isValid ? callbackUrl : '/grupos'

  const locale = await getLocale()
  const dict = await getDictionary(locale)

  return (
    <main
      className="min-h-screen flex items-center justify-center px-4"
      style={{ background: 'var(--bg)' }}
    >
      <div className="w-full max-w-sm space-y-6 text-center">
        <div>
          <div className="text-5xl mb-3">⚽</div>
          <h1 className="text-2xl font-extrabold tracking-tight" style={{ color: 'var(--text-primary)' }}>
            {dict.login.title}
          </h1>
          <p className="text-sm mt-1" style={{ color: 'var(--text-muted)' }}>
            {dict.login.subtitle}
          </p>
        </div>

        <div
          className="rounded-2xl p-5 space-y-3"
          style={{ background: 'var(--surface)', border: '1px solid var(--border)' }}
        >
          <form
            action={async () => {
              'use server'
              await signIn('google', { redirectTo })
            }}
          >
            <button
              type="submit"
              className="w-full flex items-center justify-center gap-3 rounded-xl px-4 py-3 text-sm font-semibold transition-colors"
              style={{ background: 'var(--surface-raised)', border: '1px solid var(--border)', color: 'var(--text-primary)' }}
            >
              <svg className="w-5 h-5 shrink-0" viewBox="0 0 24 24" aria-hidden="true">
                <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"/>
                <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
              </svg>
              {dict.login.continueWithGoogle}
            </button>
          </form>

          {process.env.VERCEL_ENV === 'preview' && (
            <a
              href="/api/auth/preview-signin"
              className="block w-full text-center rounded-xl px-4 py-3 text-sm font-semibold transition-colors"
              style={{ background: 'var(--surface-raised)', border: '1px solid var(--border)', color: 'var(--text-muted)' }}
            >
              {dict.login.previewLogin}
            </a>
          )}
        </div>

        <p className="text-xs" style={{ color: 'var(--text-dimmed)' }}>
          {dict.login.terms}
        </p>
      </div>
    </main>
  )
}
```

- [ ] **Step 2: Update `src/app/(shell)/reglas/page.tsx`**

Add at the top of the component function:
```tsx
import { getLocale, getDictionary } from '@/lib/i18n'
```

And at the start of `ReglasPage()`:
```tsx
const locale = await getLocale()
const dict = await getDictionary(locale)
```

Then replace every hardcoded string with `dict.reglas.*`. The `metadata` object can use a static title or be made dynamic via `generateMetadata`. For simplicity keep static metadata for now:

```tsx
export const metadata = {
  title: 'Prode Mundial 2026',
}

export default async function ReglasPage() {
  const locale = await getLocale()
  const dict = await getDictionary(locale)
  const r = dict.reglas

  return (
    <div className="max-w-2xl mx-auto space-y-6">
      <div>
        <h1 className="text-xl font-extrabold tracking-tight" style={{ color: 'var(--text-primary)' }}>
          {r.title}
        </h1>
        <p className="text-sm mt-1" style={{ color: 'var(--text-muted)' }}>
          {r.subtitle}
        </p>
      </div>

      <Section title={r.sectionGroups}>
        <ScoreRow points={5} label={r.exactResult} description={r.exactResultDesc} highlight />
        <ScoreRow points={3} label={r.correctPlusOne} description={r.correctPlusOneDesc} />
        <ScoreRow points={2} label={r.correctResult} description={r.correctResultDesc} />
        <ScoreRow points={0} label={r.wrongResult} description={r.wrongResultDesc} />
      </Section>

      <Section title={r.sectionKnockout} subtitle={r.sectionKnockoutSubtitle}>
        <ScoreRow points={7} label={r.exactPlusWinner} description={r.exactPlusWinnerDesc} highlight />
        <ScoreRow points={5} label={r.exactNoWinner} description={r.exactNoWinnerDesc} />
        <ScoreRow points={4} label={r.correctPlusWinner} description={r.correctPlusWinnerDesc} />
        <ScoreRow points={2} label={r.onlyCorrect} description={r.onlyCorrectDesc} />
        <ScoreRow points={0} label={r.wrongResult} description={r.wrongResultDesc} />
        <p className="text-xs pt-3 border-t mt-2" style={{ color: 'var(--text-muted)', borderColor: 'var(--border)' }}>
          {r.bonusNote}
        </p>
      </Section>

      <Section title={r.sectionSummary}>
        <table className="w-full text-sm">
          <thead>
            <tr className="text-left border-b" style={{ borderColor: 'var(--border)' }}>
              <th className="pb-2 text-xs uppercase tracking-wide" style={{ color: 'var(--text-muted)' }}>{r.colCondition}</th>
              <th className="pb-2 text-xs uppercase tracking-wide text-right" style={{ color: 'var(--text-muted)' }}>{r.colPts}</th>
            </tr>
          </thead>
          <tbody style={{ color: 'var(--text-primary)' }}>
            <tr className="border-b" style={{ borderColor: 'var(--border)' }}>
              <td className="py-2 text-sm" style={{ color: 'var(--text-primary)' }}>{r.baseResult}</td>
              <td className="py-2 text-right font-mono font-bold" style={{ color: 'var(--accent)' }}>+2</td>
            </tr>
            <tr className="border-b" style={{ borderColor: 'var(--border)' }}>
              <td className="py-2 text-xs pl-3" colSpan={2} style={{ color: 'var(--text-muted)' }}>
                {r.bonusesNote}
              </td>
            </tr>
            {[
              [r.homeGoalExact, '+1'],
              [r.awayGoalExact, '+1'],
              [r.bothGoalsBonus, '+1'],
              [r.winnerBonus, '+2'],
            ].map(([label, pts]) => (
              <tr key={label} className="border-b" style={{ borderColor: 'var(--border)' }}>
                <td className="py-2 text-sm pl-3" style={{ color: 'var(--text-primary)' }}>{label}</td>
                <td className="py-2 text-right font-mono font-bold" style={{ color: 'var(--accent)' }}>{pts}</td>
              </tr>
            ))}
            <tr>
              <td className="py-2 font-semibold">{r.maxPoints}</td>
              <td className="py-2 text-right font-mono font-bold" style={{ color: 'var(--accent)' }}>5 / 7</td>
            </tr>
          </tbody>
        </table>
      </Section>
    </div>
  )
}

// Section and ScoreRow helper components remain unchanged (they receive strings as props)
```

- [ ] **Step 3: Update `src/app/(shell)/torneo/page.tsx`**

Add imports:
```tsx
import { getLocale, getDictionary } from '@/lib/i18n'
import type { Dictionary } from '@/lib/i18n'
```

Inside `TorneoPage`, add at the top of the function:
```tsx
const locale = await getLocale()
const dict = await getDictionary(locale)
```

Replace the `STAGE_LABELS` constant:
```tsx
function getStageLabels(dict: Dictionary): Record<MatchStage, string> {
  return {
    GROUP:         dict.torneo.stageGroup,
    ROUND_OF_32:   dict.torneo.stageRound32,
    ROUND_OF_16:   dict.torneo.stageRound16,
    QUARTER_FINAL: dict.torneo.stageQuarter,
    SEMI_FINAL:    dict.torneo.stageSemi,
    THIRD_PLACE:   dict.torneo.stageThird,
    FINAL:         dict.torneo.stageFinal,
  }
}
```

Use it inside the component as `const STAGE_LABELS = getStageLabels(dict)`.

Add `dict` prop to all `<MatchCard ... dict={dict} />` calls.

- [ ] **Step 4: Update `src/app/(protected)/grupos/page.tsx`**

Add imports and `const dict = await getDictionary(await getLocale())` at the top. Replace strings:

- `"Mis grupos"` → `dict.grupos.title`
- `"Bienvenido, {session.user.name}"` → `t(dict.grupos.welcome, { name: session?.user?.name ?? '' })`  
  Import `t` from `@/lib/i18n`.
- `"Todavía no pertenecés a ningún grupo"` → `dict.grupos.emptyTitle`
- `"Creá uno o pedile a alguien el código de invitación"` → `dict.grupos.emptySubtitle`
- `"Crear grupo"` → `dict.grupos.createTitle`
- `placeholder="Nombre del grupo"` → `placeholder={dict.grupos.createPlaceholder}`
- `"Crear"` button → `dict.grupos.createButton`
- `"Unirse con código"` → `dict.grupos.joinTitle`
- `placeholder="Código de invitación"` → `placeholder={dict.grupos.joinPlaceholder}`
- `"Unirse"` button → `dict.grupos.joinButton`
- Plural members: `group._count.members === 1 ? dict.grupos.memberSingular : dict.grupos.memberPlural`

- [ ] **Step 5: Update `src/app/(protected)/grupos/[id]/page.tsx`**

Add imports and dict. Replace strings:

- `"← Mis grupos"` → `dict.grupoDetail.back`
- Plural members: `group.members.length === 1 ? dict.grupoDetail.memberSingular : dict.grupoDetail.memberPlural`
- `"Tabla de posiciones"` → `dict.grupoDetail.standingsTitle`
- Table headers `['#', 'Participante', 'Pts', 'Aciertos']` → `[dict.grupoDetail.tableRank, dict.grupoDetail.tableParticipant, dict.grupoDetail.tablePoints, dict.grupoDetail.tableCorrect]`
- Add `dict={dict}` to `<MatchCard />` if this page renders match cards (check the full file — it may not). If it does not render MatchCard, skip.

- [ ] **Step 6: Update `src/app/(protected)/perfil/page.tsx`**

Add imports and dict. Replace strings:

- `"Perfil"` → `dict.perfil.title`
- `"Notificaciones"` → `dict.perfil.notificationsTitle`
- `"Recordatorios y resumen diario"` → `dict.perfil.notificationsLabel`
- The long description → `dict.perfil.notificationsDescription`
- `"Guardar"` button → `dict.perfil.saveButton`

- [ ] **Step 7: Update `src/app/(shell)/invite/[code]/page.tsx`**

Add imports and dict. Replace strings:

- `"Link inválido"` → `dict.invite.invalidTitle`
- `"Este link de invitación no es válido…"` → `dict.invite.invalidDescription`
- `"Ver el torneo →"` → `dict.invite.viewTournament`
- Plural: `memberCount === 1 ? dict.invite.memberSingular : dict.invite.memberPlural`
- `"Uniéndote a este grupo…"` → `dict.invite.loggedInDescription`
- `"Iniciá sesión para unirte a este grupo…"` → `dict.invite.guestDescription`
- `"Unirse al grupo"` button → `dict.invite.joinButton`
- `"Iniciar sesión para unirte"` link → `dict.invite.loginToJoin`

- [ ] **Step 8: Update `src/components/torneo-filters.tsx`**

`TorneoFilters` is a Client Component with hardcoded Spanish strings. Add `useDict` and replace them:

```tsx
import { useDict } from '@/components/locale-provider'
```

Inside `TorneoFilters`, add:
```tsx
const dict = useDict()
```

Replace the three hardcoded `BADGE_LABELS`, `STAGE_LABELS`, and filter UI strings:

```tsx
// Replace BADGE_LABELS and STAGE_LABELS constants with dict lookups:
const BADGE_LABELS: Record<GroupStatus, string> = {
  complete:       dict.torneo.badgeComplete,
  actionRequired: dict.torneo.badgeActionRequired,
  missed:         dict.torneo.badgeMissed,
}

const STAGE_LABELS: Record<MatchStage, string> = {
  GROUP:         dict.torneo.stageGroup,
  ROUND_OF_32:   dict.torneo.stageRound32,
  ROUND_OF_16:   dict.torneo.stageRound16,
  QUARTER_FINAL: dict.torneo.stageQuarter,
  SEMI_FINAL:    dict.torneo.stageSemi,
  THIRD_PLACE:   dict.torneo.stageThird,
  FINAL:         dict.torneo.stageFinal,
}
```

Move these two objects inside the `TorneoFilters` function body (after `const dict = useDict()`), removing the module-level declarations.

Replace the two `'Todos'` literals:
```tsx
// Both occurrences of:  >Todos<
// Replace with:         >{dict.torneo.filterAll}<
```

Replace `'Grupo:'`:
```tsx
// Before: <span className="text-xs mr-1" ...>Grupo:</span>
// After:  <span className="text-xs mr-1" ...>{dict.torneo.filterGroupLabel}</span>
```

- [ ] **Step 9: Run the full test suite**

```bash
cd /Users/mariano/Work/prode && npx vitest run
```

Expected: all tests PASS. Fix any TypeScript or import errors before committing.

- [ ] **Step 10: Commit**

```bash
git add src/app/login/page.tsx \
        "src/app/(shell)/reglas/page.tsx" \
        "src/app/(shell)/torneo/page.tsx" \
        "src/app/(protected)/grupos/page.tsx" \
        "src/app/(protected)/grupos/[id]/page.tsx" \
        "src/app/(protected)/perfil/page.tsx" \
        "src/app/(shell)/invite/[code]/page.tsx" \
        src/components/torneo-filters.tsx
git commit -m "feat: translate server pages and TorneoFilters"
```

---

## Task 12: Final verification and push

- [ ] **Step 1: Run full test suite**

```bash
cd /Users/mariano/Work/prode && npx vitest run
```

Expected: all tests PASS

- [ ] **Step 2: Build check**

```bash
cd /Users/mariano/Work/prode && npx next build 2>&1 | tail -30
```

Expected: no TypeScript errors, successful build. Fix any type errors (most likely missing `dict` prop on `MatchCard`).

- [ ] **Step 3: Push the branch**

```bash
git push -u origin feat/i18n
```

---

## Quick reference: key patterns used throughout

**Server Component** (page or layout):
```tsx
import { getLocale, getDictionary } from '@/lib/i18n'
const locale = await getLocale()
const dict = await getDictionary(locale)
// use dict.section.key directly
```

**Client Component**:
```tsx
import { useDict } from '@/components/locale-provider'
const dict = useDict()
// use dict.section.key directly
```

**String interpolation**:
```tsx
import { t } from '@/lib/i18n'
t(dict.grupos.welcome, { name: userName }) // "Bienvenido, Ana"
```

**Plural**:
```tsx
count === 1 ? dict.grupos.memberSingular : dict.grupos.memberPlural
```
